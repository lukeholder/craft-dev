name: PR Check
on:
  pull_request:
    branches:
      - master
      - staging
      - dev
jobs:
  check:
      name: Deploy apps
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - run: make -C docker build
